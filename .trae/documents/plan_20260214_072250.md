## Sepet Üselleri Düzrün Göreltme Planı

### Sorun:
Sepette ürün görselleri yerine emoji gösteriliyor (🥜, 🌰, 🥔)

### Yapılacak Değişiklikler:

1. **SideCart.tsx** - Görsel alanını güncelle
   - `item.product.images` dizisinden ilk görseli al
   - `<img>` etiketi ile göster
   - Görsel yoksa emoji fallback

2. **sepet/page.tsx** - Görsel alanını güncelle  
   - Aynı şekilde gerçek görselleri göster
   - Fallback olarak emoji kullan

### Örnek kod:
```jsx
// Yerine:
<div className="w-16 h-16 bg-white rounded-xl...">
  {item.product.category === "fistik-ezmesi" && "🥜"}
</div>

// Şunu kullan:
{item.product.images && item.product.images.length > 0 ? (
  <img src={item.product.images[0]} alt={item.product.name} className="w-full h-full object-cover" />
) : (
  <span className="text-2xl">
    {item.product.category === "fistik-ezmesi" && "🥜"}
  </span>
)}
```